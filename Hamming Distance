#Finds a pattern that appears in a genome with at most d mismatches
def HammingDistance(p, q):
    count = 0
    n = len(p)
    for i in range(n):
        if p[i] != q[i]:
            count += 1
    return count
